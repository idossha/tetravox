pnpm-lock.yaml
Cargo.lock
target/
node_modules/
packages/wasm/pkg/
dist/
out/
test-results/
playwright-report/
README.md

# The contract and its measured reference values are hand-formatted and must not be reflowed.
docs/
AGENTS.md
scripts/refvalues/

# Generated ground truth: `manifest.json` is written by scripts/gen-fixtures.py with a
# fixed 1-space indent and sorted keys, so it stays diffable across regenerations. A
# reflow would make every rerun look like a content change.
testdata/

# Generated ground truth, as above: `scripts/reference/glyphs.py` writes this with SimNIBS + numpy
# and it is compared element by element, never read by a human. Reflowing 1,397 elements of f64
# would make every regeneration a whole-file diff.
packages/engine/test/fixtures/

# Agent worktrees of other Claude sessions.
.claude/

# VitePress docs site (website/): src/ is regenerated from docs/*.md (itself unformatted, above) by
# scripts/sync.mjs and scripts/gallery.mjs, and cache/dist are build output. website/.vitepress/config.ts,
# theme/ and scripts/ are hand-written and stay linted.
website/src/
website/.vitepress/cache/
website/.vitepress/dist/
