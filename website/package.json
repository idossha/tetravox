{
  "name": "@tetravox/website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Tetravox documentation site",
  "scripts": {
    "dev": "node scripts/sync.mjs && node scripts/gallery.mjs && vitepress dev",
    "build": "node scripts/sync.mjs && node scripts/gallery.mjs && node scripts/check-images.mjs && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
