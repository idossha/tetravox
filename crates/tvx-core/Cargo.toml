[package]
name = "tvx-core"
description = "Tetravox shared types: Plane, BitMask, Field, LabelTable, Aabb, Error, ProgressSink (ARCHITECTURE.md §6.0)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
thiserror.workspace = true
# `Aabb` is a member of `tvx_geom::IsolateCriteria`, which §6.3 derives `serde::Deserialize` on.
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
